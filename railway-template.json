{
  "name": "SideRail",
  "description": "Xray-core VPN management panel — VLESS/VMess/Trojan over WS, XHTTP and HTTPUpgrade. TLS terminated at the Railway edge on port 443.",
  "tags": ["vpn", "xray", "proxy", "nodejs", "react"],
  "services": [
    {
      "name": "siderail",
      "source": {
        "type": "dockerfile",
        "path": "Dockerfile"
      },
      "variables": {
        "JWT_SECRET": {
          "description": "Secret used to sign admin session cookies. Set a long random string.",
          "generator": "secret"
        },
        "XRAY_VERSION": {
          "description": "Xray-core release to download on first boot.",
          "default": "v26.9.9"
        },
        "SIDERAIL_DATA_DIR": {
          "description": "Persistent data directory. Mount a volume here.",
          "default": "/data"
        }
      },
      "volumes": [
        {
          "mountPath": "/data"
        }
      ]
    }
  ]
}
